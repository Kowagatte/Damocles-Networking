namespace Networking {
    
    public enum NamedPackets : byte {
        PlayerInput = 0,
        Info = 1,
        LoginRequest = 2,
        CreateAccountRequest = 3,
        GameObjectMove = 1
    }
}